# Upstream WebF `use_cases` folder (external repo).
# Used by: `just use-cases-build` / `just use-cases-copy`.
#
# Tips:
# - Use forward slashes on Windows for best compatibility with `sh`.
# - The scripts expect a `build/` folder inside this directory.
#
# Example:
WEBF_USE_CASES_DIR=
