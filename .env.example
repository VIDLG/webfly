# Web project source directory (external repo or local web project).
# Used by: `just web-build` / `just web-copy` / `just web-refresh`.
#
# Tips:
# - Use forward slashes on Windows for best compatibility with `sh`.
# - The scripts expect a `build/` or `dist/` folder inside this directory after building.
# - Supports pnpm, npm, yarn, and bun package managers.
#
# Example:
WEB_BUILD_SRC_DIR=
